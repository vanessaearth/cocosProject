cc.Class({
    extends: cc.Component,

    properties: {
        num:100,
        mcount:100,
        pointX:100,
        pointY:100,
        isempty:false
     },

    // use this for initialization
    onLoad: function () {
       
    },

    // called every frame
    update: function (dt) {

    },
});
